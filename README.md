Mod that automatically goes throught all the asset bundles and checks if bundle contains scenes via bundle.GetAllScenePaths().
If it contains scenes it dumps all "PersistentBoolItems", "PersistentIntItems" and "GeoRocks"

Just load Mod via BepInEx and press F7 in the Main Menu. There should be a little GUI in the top right if everything is working fine.
